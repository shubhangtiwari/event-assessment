[project]
name = "event-assessment"
version = "0.1.0"
description = "Claude Enablement pre-event assessment"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "openpyxl>=3.1",
    "segno>=1.6",
]
